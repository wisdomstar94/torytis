import { getCategoryInfo } from "@wisdomstar94/torytis-utils";

window.addEventListener("load", () => {
  // ...
  const categoryInfo = getCategoryInfo();
  console.log(`...categoryInfo`, categoryInfo);
});
